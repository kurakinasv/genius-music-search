import Button from '@components/Button';
import Input from '@components/Input';

const SearchPage = () => {
  return (
    <div>
      Search page
      <Input />
      <Button />
    </div>
  );
};

export default SearchPage;
